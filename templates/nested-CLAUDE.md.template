# `<path>` conventions

Loaded automatically when working in this directory (progressive disclosure).
**Caveat:** Path-scoped trigger fires on file READ, not Write/Create — Claude
Code issue #23478. If creating a brand-new file here, read a sibling first.

- **Invariant 1** — something a senior engineer would get wrong without being told. Keep concrete; cite file paths where applicable.
- **Invariant 2** — another load-bearing fact. Avoid restating canonical rule content — point to it instead.
- **Invariant 3** — ...
- **Invariant 4** — ...
- **Invariant 5** — ...
- **Invariant 6** — ...

→ Authoritative rules: `.claude/rules/<file>.md`, `.claude/rules/<other-file>.md`

<!-- Target: 6-10 bullets, under 40 lines total. No code blocks longer than 3 lines. Every bullet should pass the Deletion Test: removing it would cause Claude to make a real mistake. -->
